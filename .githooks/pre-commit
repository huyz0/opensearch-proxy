#!/usr/bin/env bash
# Pre-commit gate: the tree must be green before a commit lands.
# Mirrors CI. Bypass in genuine emergencies with `git commit --no-verify`.
set -euo pipefail

echo "pre-commit: running cargo xtask ci ..."
cargo xtask ci
echo "pre-commit: ok"
